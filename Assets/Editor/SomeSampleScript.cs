using UnityEngine;
using System.Collections;

public class SomeSampleScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	//Comment to test if editing in browser will affect the local script.
	}
}
// F*c* this s*i*
